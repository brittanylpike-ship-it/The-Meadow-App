export function getAuthGateCopy(mode, isCreating) {
  void mode;
  void isCreating;

  return {
    subtitle: "A private place that remembers what you leave with care.",
    title: "Begin here",
    primaryAction: "Enter The Meadow",
    secondaryAction: "I already have a place here",
  };
}
