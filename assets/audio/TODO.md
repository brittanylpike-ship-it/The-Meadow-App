# Meadow Audio Placeholders

These empty `.mp3` files are placeholders only.

TODO: replace every file in `assets/audio/ambient` and `assets/audio/interactions`
with final audio assets from the design team before release audio QA.
